# =============================================================================
# Contents Researcher - 환경변수 설정 예시
# =============================================================================
# 사용법: 이 파일을 .env로 복사한 후 실제 값으로 수정하세요.
#   cp .env.example .env
#
# 주의: .env 파일은 절대 Git에 커밋하지 마세요!
# =============================================================================

# ---------------------------------------------------------------------------
# 데이터베이스 설정 (SQLite — Docker 불필요)
# ---------------------------------------------------------------------------
# aiosqlite 드라이버를 사용하는 비동기 연결 URL
# data/ 디렉토리에 DB 파일이 자동 생성됨
DATABASE_URL=sqlite+aiosqlite:///data/contents_researcher.db

# ---------------------------------------------------------------------------
# 외부 API 키
# ---------------------------------------------------------------------------
# YouTube Data API v3 키 (Google Cloud Console에서 발급)
YOUTUBE_API_KEY=your_youtube_api_key
